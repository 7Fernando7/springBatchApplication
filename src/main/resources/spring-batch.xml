<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:batch="http://www.springframework.org/schema/batch"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"

       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           https://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/batch
           https://www.springframework.org/schema/batch/spring-batch.xsd">

    <import resource="spring.xml"/>

    <!-- CSV ITEM READER -->

    <bean id="csvItemReader" class="org.springframework.batch.item.file.FlatFileItemReader">

        <property name="resource"
                  value="read/tickets.json" />

        <property name="lineMapper">
            <bean class="org.springframework.batch.item.file.mapping.DefaultLineMapper">

                <property name="lineTokenizer">
                    <bean class="org.springframework.batch.item.file.transform.DelimitedLineTokenizer">

                        <property name="names"
                                  value="name, ticket_number, route, ticket_price" />

                    </bean>
                </property>

                <property name="fieldSetMapper">
                    <bean class="org.springframework.batch.item.file.mapping.BeanWrapperFieldSetMapper">
                        <property name="prototypeBeanName" value="flightTicket"/>
                    </bean>
                </property>

            </bean>
        </property>

        <property name="linesToSkip"
                  value="1"/>
    </bean>

    <!-- JSON ITEM READER -->

    <bean id="jsonItemReader"
          class="org.springframework.batch.item.json.JsonItemReader">

        <property name="resource" value="classpath:read/tickets.json"/>

        <property name="jsonObjectReader">
            <bean class="org.springframework.batch.item.json.JacksonJsonObjectReader">
                <constructor-arg value="com.skillsoft.model.FlightTicket"/>
            </bean>
        </property>

    </bean>

    <!-- MODEL -->

    <bean id="flightTicket"
          class="com.skillsoft.model.FlightTicket"
          scope="prototype"/>


    <!-- PROCESSOR -->

    <bean id="itemProcessor"
          class="com.skillsoft.service.CustomItemProcessor"/>


    <!-- WRITER -->

    <bean id="itemWriter"
          class="org.springframework.batch.item.xml.StaxEventItemWriter">

        <property name="resource"
                  value="file:write/xml/result.xml"/>

        <property name="marshaller"
                  ref="recordMarshaller"/>

        <property name="rootTagName"
                  value="ticketRecords"/>

    </bean>

    <!-- JAXB MARSHALLER -->

    <bean id="recordMarshaller"
          class="org.springframework.oxm.jaxb.Jaxb2Marshaller">

        <property name="classesToBeBound">
            <list>
                <value>com.skillsoft.model.FlightTicket</value>
            </list>
        </property>

    </bean>

    <!-- JOB -->

    <batch:job id="CSVtoXML">

        <batch:step id="step1">

            <batch:tasklet>

                <!-- Cambia aquí el reader -->
                <!-- reader="csvItemReader" -->
                <!-- reader="jsonItemReader" -->

                <!--<batch:chunk reader="csvItemReader"
                            writer="itemWriter"
                            processor="itemProcessor"
                            commit-interval="10"/>-->

                <batch:chunk reader="jsonItemReader"
                             writer="itemWriter"
                             processor="itemProcessor"
                             commit-interval="10"/>

           </batch:tasklet>

       </batch:step>

   </batch:job>

</beans>